#ifndef TIMER_H#define TIMER_H/** * http://www.osdever.net/bkerndev/Docs/pit.htm */#include "common.h"/** timer_handler: * Handle the system timer (callback). By default, the timer fires at 18.222Hz * Why? Some engineer at IBM must've been smoking something funky. * * @param r System registers */void timer_handler(registers r);/** timer_init: * Initialize the programmable interval timer (PIT) chip. * * @param frequency  The timer frequency (in Hz) */void timer_init(u32 frequency);#endif /* TIMER_H */