#ifndef KPRINTF_H#define KPRINTF_H#include "common.h"extern char *fb;extern u32 cursor_pos;/** move_cursor: * Moves the cursor of the framebuffer to the given position. * * @param pos The new position of the cursor */void move_cursor(u16 pos);/** read_cell: *  Read a cell from the framebuffer grid for a given column and row and *  return the character from it. * *  @param row  The row number *  @param col  The column number */char read_cell(u32 row, u32 col);/** write_cell: *  Writes a character with the given foreground and background to position i *  in the framebuffer. * *  @param i  The location in the framebuffer *  @param c  The character *  @param fg The foreground color *  @param bg The background color */void write_cell(u32 i, char c, u8 fg, u8 bg);/** new_line: *  Shift the cursor position of one line and return to row 0. * *  @param pos  The actual cursor position */void new_line(u32 pos);/** write_at: *  Writes a character c to position pos in the framebuffer. * *  @param pos  The location in the framebuffer *  @param c  The character */void write_at(u32 pos, char c);/** write_char: *  Writes a character in the framebuffer. * *  @param c  The character */void write_char(char c);/** screen_write: *  Writes a string in the screen (print func). * *  @param str[]  The string */void kprint(char str[]);/** scroll: *  Scroll the screen (shift the framebuffer). * */void scroll();/** clear_screen: *  Clear the screen. * */void clear_screen();#endif /* KPRINTF_H */